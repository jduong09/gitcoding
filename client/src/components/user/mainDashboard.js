import React from 'react';
import DashboardCalendar from '../date/dashboardCalendar';
import SubscriptionDetail from '../subscription/subscriptionDetail';
import UpdateSubscription from '../subscription/updateSubscription';
import CreateSubscription from '../subscription/createSubscription';

// xs < 576px
// sm >= 576px
// md >= 768px
// lg >= 992px
// xl >= 1200px
// xxl >= 1400px
/**
 * House logic between mobile and desktop. 
 * Mobile: Calendar, and SubscriptionDetail, offcanvas Update/Create Subscription Forms.
 * Desktop: Calendar, SubscriptionDetail, Update/Create Subscription Forms. No offcanvas.
 */

const MainDashboard = () => {

};

export default MainDashboard;